server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/fhburgenland-bswe/ha-microservices-architecture-gruppe9
          default-label: main
          search-paths:
            - config-repo
          username:
  application:
    name: config-server