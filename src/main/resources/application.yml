server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/abelhorvath04/config-server.git
          default-label: main
          search-paths:
            - config-repo
          username:
  application:
    name: config-server